services:
  postgres:
    image: postgres
    container_name: postgres
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: root
      PGTZ: UTC
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
